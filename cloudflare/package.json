{
  "name": "simonsealsapi-cloudflare",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "cf": "wrangler",
    "dev": "wrangler dev",
    "test": "node --test tests/*.test.mjs",
    "check": "wrangler deploy --dry-run --outdir .wrangler/check",
    "configure-db": "node scripts/configure-db.mjs",
    "deploy": "node scripts/deploy.mjs"
  },
  "devDependencies": {
    "node": "22.23.2",
    "wrangler": "4.137.0",
    "miniflare": "5.20260921.0-alpha"
  }
}
